<%= form_for(@media_library) do |f| %>
  <% if @media_library.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@media_library.errors.count, "error") %> prohibited this media_library from being saved:</h2>

      <ul>
      <% @media_library.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
